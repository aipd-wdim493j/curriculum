#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../eco/bin/eco" "$@"
  ret=$?
else 
  node  "$basedir/../eco/bin/eco" "$@"
  ret=$?
fi
exit $ret
